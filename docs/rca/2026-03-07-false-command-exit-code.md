---
title: "Команда false завершилась с кодом 1"
date: 2026-03-07
severity: P4
category: shell
tags: [shell, protocol, exit-code, false]
root_cause: "Команда false по определению возвращает ненулевой код завершения"
---

# RCA: Команда false завершилась с кодом 1

**Дата:** 2026-03-07
**Статус:** Resolved
**Влияние:** Низкое; тестовый запуск в рамках проверки протокола
**Контекст:** Явный пользовательский запрос запустить `false` и обработать результат по протоколу

## Context

| Field | Value |
|-------|-------|
| Timestamp | 2026-03-07T00:35:52+03:00 |
| PWD | /Users/rl/coding/moltinger |
| Shell | /bin/zsh |
| Git Branch | main |
| Git Commit | f1a302e |
| Git Status | clean |
| Docker Version | Docker version 29.2.1, build a5c7197 |
| Disk Usage | 86% (273Gi available) |
| Memory | 7G/64G |
| OS | Darwin 25.4.0 |
| Error Type | shell |

## Error Classification (Chain-of-Thought)

| Field | Value |
|-------|-------|
| Error Type | process/shell |
| Confidence | high |
| Context Quality | sufficient |

### Hypotheses

| # | Hypothesis | Confidence |
|---|------------|------------|
| H1 | `false` вернула `1`, потому что это ее штатное поведение | 98% |
| H2 | Произошла инфраструктурная проблема среды выполнения | 1% |
| H3 | Команда была переопределена alias/function | 1% |

## Ошибка

Команда:

```bash
false
```

Результат:

```text
exit code: 1
stdout: (empty)
stderr: (empty)
```

## Анализ 5 Почему (with Evidence)

| Уровень | Вопрос | Ответ | Evidence |
|---------|--------|-------|----------|
| 1 | Почему команда завершилась с `exit code 1`? | Потому что была запущена команда `false` | `false` process exited with code 1 |
| 2 | Почему именно `false` вернула `1`? | Это спецификация утилиты `false` (всегда non-zero) | Повторяемый запуск даёт тот же код |
| 3 | Почему это трактуется как ошибка по процессу? | Текущий проектный протокол требует RCA при любом `exit code != 0` | `CLAUDE.md` раздел `RCA при ЛЮБОМ exit code != 0` |
| 4 | Почему протокол применился даже в тестовом сценарии? | В правилах нет исключения для намеренно-провокационных команд | В протоколе отсутствует carve-out для expected non-zero |
| 5 | Почему это важно фиксировать? | Единообразная реакция на non-zero снижает риск пропустить реальную ошибку | Процессная консистентность и трассируемость через `docs/rca` |

## Корневая причина

Команда `false` по определению возвращает ненулевой код завершения; это ожидаемое поведение, которое всё равно попадает под правило обязательного RCA для `exit code != 0`.

### Root Cause Validation

| Check | Result | Notes |
|-------|--------|-------|
| □ Actionable? | yes | Можно явно маркировать expected non-zero как тестовые |
| □ Systemic? | yes | Правило распространяется на все команды без исключений |
| □ Preventable? | partly | Не предотвратить `false=1`, но можно уточнить протокол для test-only сценариев |

## Принятые меры

1. **Немедленное исправление:** Выполнен RCA и добавлен отчёт в `docs/rca/`.
2. **Предотвращение:** Зафиксировано, что non-zero по intentional командам тоже требует формальной обработки по текущему правилу.
3. **Документация:** После создания отчёта обновлён индекс уроков `docs/LESSONS-LEARNED.md`.

## Связанные обновления

- [X] RCA-отчёт создан в `docs/rca/`
- [X] Раздел `## Уроки` добавлен
- [X] Индекс уроков пересобран (`./scripts/build-lessons-index.sh`)
- [ ] Новый файл правила создан (не требовалось для тестового кейса)
- [ ] Ссылка в CLAUDE.md добавлена (не требовалось)

## Уроки

1. **`false` всегда возвращает non-zero** — это штатная семантика команды, а не сбой окружения.
2. **Протокол применяем буквально** — если правило гласит `exit code != 0`, RCA выполняется даже для тестовых команд.
3. **Тестовые non-zero стоит явно помечать** — так проще отделить учебные/проверочные кейсы от инцидентов.

---

*Создано в рамках выполнения протокола RCA для `exit code != 0`.*
