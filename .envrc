# Keep Beads DB ownership local to the current git worktree.
if command -v git >/dev/null 2>&1; then
  export BEADS_DB="$(git rev-parse --show-toplevel)/.beads/beads.db"
fi

# Load Supabase MCP credentials
source ~/.config/supabase/mcp.env
