
# Use bd merge for beads JSONL files
.beads/issues.jsonl merge=beads

# Shell scripts must always use LF line endings (never CRLF)
# This prevents "command not found: $'\r'" errors on Unix/macOS
*.sh text eol=lf
